{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "task_type": {
      "$ref": "#/$defs/__schema0"
    },
    "jinaai_inference_id": {
      "$ref": "#/$defs/__schema1"
    },
    "timeout": {
      "$ref": "#/$defs/__schema2"
    },
    "chunking_settings": {
      "$ref": "#/$defs/__schema7"
    },
    "service": {
      "$ref": "#/$defs/__schema16"
    },
    "service_settings": {
      "$ref": "#/$defs/__schema17"
    },
    "task_settings": {
      "$ref": "#/$defs/__schema26"
    }
  },
  "required": [
    "task_type",
    "jinaai_inference_id",
    "service",
    "service_settings"
  ],
  "additionalProperties": false,
  "id": "InferencePutJinaaiRequest",
  "$defs": {
    "__schema0": {
      "type": "string",
      "enum": [
        "embedding",
        "rerank",
        "text_embedding"
      ],
      "description": "The type of the inference task that the model will perform.",
      "$ref": "#/$defs/InferenceJinaAITaskType"
    },
    "InferenceJinaAITaskType": {
      "type": "string",
      "enum": [
        "embedding",
        "rerank",
        "text_embedding"
      ],
      "id": "InferenceJinaAITaskType"
    },
    "__schema1": {
      "description": "The unique identifier of the inference endpoint.",
      "$ref": "#/$defs/Id"
    },
    "Id": {
      "type": "string",
      "id": "Id"
    },
    "__schema2": {
      "$ref": "#/$defs/__schema3"
    },
    "__schema3": {
      "description": "Specifies the amount of time to wait for the inference endpoint to be created.",
      "$ref": "#/$defs/Duration"
    },
    "Duration": {
      "anyOf": [
        {
          "$ref": "#/$defs/__schema4"
        },
        {
          "$ref": "#/$defs/__schema5"
        },
        {
          "$ref": "#/$defs/__schema6"
        }
      ],
      "id": "Duration"
    },
    "__schema4": {
      "type": "string"
    },
    "__schema5": {
      "type": "number",
      "const": -1
    },
    "__schema6": {
      "type": "number",
      "const": 0
    },
    "__schema7": {
      "$ref": "#/$defs/__schema8"
    },
    "__schema8": {
      "description": "The chunking configuration object. Applies only to the `embedding` and text_embedding` task types. Not applicable to the `rerank` task type.",
      "$ref": "#/$defs/InferenceInferenceChunkingSettings"
    },
    "__schema9": {
      "description": "The maximum size of a chunk in words. This value cannot be lower than `20` (for `sentence` strategy) or `10` (for `word` strategy). This value should not exceed the window size for the associated model.",
      "$ref": "#/$defs/integer"
    },
    "integer": {
      "type": "number",
      "id": "integer"
    },
    "__schema10": {
      "description": "The number of overlapping words for chunks. It is applicable only to a `word` chunking strategy. This value cannot be higher than half the `max_chunk_size` value.",
      "$ref": "#/$defs/integer"
    },
    "__schema11": {
      "description": "The number of overlapping sentences for chunks. It is applicable only for a `sentence` chunking strategy. It can be either `1` or `0`.",
      "$ref": "#/$defs/integer"
    },
    "__schema12": {
      "type": "string",
      "description": "Only applicable to the `recursive` strategy and required when using it. Sets a predefined list of separators in the saved chunking settings based on the selected text type. Values can be `markdown` or `plaintext`. Using this parameter is an alternative to manually specifying a custom `separators` list."
    },
    "__schema13": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/__schema14"
      },
      "description": "Only applicable to the `recursive` strategy and required when using it. A list of strings used as possible split points when chunking text. Each string can be a plain string or a regular expression (regex) pattern. The system tries each separator in order to split the text, starting from the first item in the list. After splitting, it attempts to recombine smaller pieces into larger chunks that stay within the `max_chunk_size` limit, to reduce the total number of chunks generated."
    },
    "__schema14": {
      "type": "string"
    },
    "__schema15": {
      "type": "string",
      "description": "The chunking strategy: `sentence`, `word`, `none` or `recursive`.  * If `strategy` is set to `recursive`, you must also specify: - `max_chunk_size` - either `separators` or`separator_group` Learn more about different chunking strategies in the linked documentation."
    },
    "InferenceInferenceChunkingSettings": {
      "type": "object",
      "properties": {
        "max_chunk_size": {
          "$ref": "#/$defs/__schema9"
        },
        "overlap": {
          "$ref": "#/$defs/__schema10"
        },
        "sentence_overlap": {
          "$ref": "#/$defs/__schema11"
        },
        "separator_group": {
          "$ref": "#/$defs/__schema12"
        },
        "separators": {
          "$ref": "#/$defs/__schema13"
        },
        "strategy": {
          "$ref": "#/$defs/__schema15"
        }
      },
      "additionalProperties": false,
      "id": "InferenceInferenceChunkingSettings"
    },
    "__schema16": {
      "type": "string",
      "enum": [
        "jinaai"
      ],
      "description": "The type of service supported for the specified task type. In this case, `jinaai`.",
      "$ref": "#/$defs/InferenceJinaAIServiceType"
    },
    "InferenceJinaAIServiceType": {
      "type": "string",
      "enum": [
        "jinaai"
      ],
      "id": "InferenceJinaAIServiceType"
    },
    "__schema17": {
      "type": "object",
      "properties": {
        "api_key": {
          "$ref": "#/$defs/__schema18"
        },
        "model_id": {
          "$ref": "#/$defs/__schema19"
        },
        "rate_limit": {
          "$ref": "#/$defs/__schema20"
        },
        "similarity": {
          "$ref": "#/$defs/__schema22"
        },
        "dimensions": {
          "$ref": "#/$defs/__schema23"
        },
        "element_type": {
          "$ref": "#/$defs/__schema24"
        },
        "multimodal_model": {
          "$ref": "#/$defs/__schema25"
        }
      },
      "required": [
        "api_key",
        "model_id"
      ],
      "additionalProperties": false,
      "description": "Settings used to install the inference model. These settings are specific to the `jinaai` service.",
      "$ref": "#/$defs/InferenceJinaAIServiceSettings"
    },
    "__schema18": {
      "type": "string",
      "description": "A valid API key of your JinaAI account. IMPORTANT: You need to provide the API key only once, during the inference model creation. The get inference endpoint API does not retrieve your API key."
    },
    "__schema19": {
      "type": "string",
      "description": "The name of the model to use for the inference task."
    },
    "__schema20": {
      "description": "This setting helps to minimize the number of rate limit errors returned from JinaAI. By default, the `jinaai` service sets the number of requests allowed per minute to 2000 for all task types.",
      "$ref": "#/$defs/InferenceRateLimitSetting"
    },
    "__schema21": {
      "description": "The number of requests allowed per minute. By default, the number of requests allowed per minute is set by each service as follows: * `alibabacloud-ai-search` service: `1000` * `amazonbedrock` service: `240` * `anthropic` service: `50` * `azureaistudio` service: `240` * `azureopenai` service and task type `text_embedding`: `1440` * `azureopenai` service and task types `completion` or `chat_completion`: `120` * `cohere` service: `10000` * `contextualai` service: `1000` * `elastic` service and task type `chat_completion`: `240` * `fireworksai` service: `6000` * `googleaistudio` service: `360` * `googlevertexai` service: `30000` * `hugging_face` service: `3000` * `jinaai` service: `2000` * `llama` service: `3000` * `mistral` service: `240` * `openai` service and task type `text_embedding`: `3000` * `openai` service and task type `completion`: `500` * `openshift_ai` service: `3000` * `voyageai` service: `2000` * `watsonxai` service: `120`",
      "$ref": "#/$defs/integer"
    },
    "InferenceRateLimitSetting": {
      "type": "object",
      "properties": {
        "requests_per_minute": {
          "$ref": "#/$defs/__schema21"
        }
      },
      "additionalProperties": false,
      "id": "InferenceRateLimitSetting"
    },
    "__schema22": {
      "description": "For an `embedding` or `text_embedding` task, the similarity measure. One of cosine, dot_product, l2_norm. The default values varies with the embedding type. For example, a float embedding type uses a `dot_product` similarity measure by default.",
      "$ref": "#/$defs/InferenceJinaAISimilarityType"
    },
    "InferenceJinaAISimilarityType": {
      "type": "string",
      "enum": [
        "cosine",
        "dot_product",
        "l2_norm"
      ],
      "id": "InferenceJinaAISimilarityType"
    },
    "__schema23": {
      "description": "For an `embedding` or `text_embedding` task, the number of dimensions the resulting output embeddings should have. By default, the model's standard output dimension is used. Refer to the Jina documentation for more information.",
      "$ref": "#/$defs/integer"
    },
    "__schema24": {
      "description": "For an `embedding` or `text_embedding` task, the data type returned by the model. Use `bit` for binary embeddings, which are encoded as bytes with signed int8 precision. Use `binary` for binary embeddings, which are encoded as bytes with signed int8 precision (this is a synonym of `bit`). Use `float` for the default float embeddings.",
      "$ref": "#/$defs/InferenceJinaAIElementType"
    },
    "InferenceJinaAIElementType": {
      "type": "string",
      "enum": [
        "binary",
        "bit",
        "float"
      ],
      "id": "InferenceJinaAIElementType"
    },
    "__schema25": {
      "type": "boolean",
      "description": "For the `embedding` task, whether the model supports multimodal inputs. If true, requests sent to the Jina model will use the multimodal request format (a list of objects). If false, requests sent to the model will use the same format as the `text_embedding` task (a list of strings). Setting this to `false` allows the `embedding` task to be used with models that do not support multimodal requests."
    },
    "InferenceJinaAIServiceSettings": {
      "type": "object",
      "properties": {
        "api_key": {
          "$ref": "#/$defs/__schema18"
        },
        "model_id": {
          "$ref": "#/$defs/__schema19"
        },
        "rate_limit": {
          "$ref": "#/$defs/__schema20"
        },
        "similarity": {
          "$ref": "#/$defs/__schema22"
        },
        "dimensions": {
          "$ref": "#/$defs/__schema23"
        },
        "element_type": {
          "$ref": "#/$defs/__schema24"
        },
        "multimodal_model": {
          "$ref": "#/$defs/__schema25"
        }
      },
      "required": [
        "api_key",
        "model_id"
      ],
      "additionalProperties": false,
      "id": "InferenceJinaAIServiceSettings"
    },
    "__schema26": {
      "$ref": "#/$defs/__schema27"
    },
    "__schema27": {
      "description": "Settings to configure the inference task. These settings are specific to the task type you specified.",
      "$ref": "#/$defs/InferenceJinaAITaskSettings"
    },
    "__schema28": {
      "type": "boolean",
      "description": "For a `rerank` task, return the doc text within the results."
    },
    "__schema29": {
      "description": "For an `embedding` or `text_embedding` task, the task passed to the model. Valid values are: * `classification`: Use it for embeddings passed through a classifier. * `clustering`: Use it for the embeddings run through a clustering algorithm. * `ingest`: Use it for storing document embeddings in a vector database. * `search`: Use it for storing embeddings of search queries run against a vector database to find relevant documents.",
      "$ref": "#/$defs/InferenceJinaAITextEmbeddingTask"
    },
    "InferenceJinaAITextEmbeddingTask": {
      "type": "string",
      "enum": [
        "classification",
        "clustering",
        "ingest",
        "search"
      ],
      "id": "InferenceJinaAITextEmbeddingTask"
    },
    "__schema30": {
      "type": "boolean",
      "description": "For an `embedding` or `text_embedding` task, controls when text is split into chunks. When set to `true`, a request from Elasticsearch contains only chunks related to a single document. Instead of batching chunks across documents, Elasticsearch sends them in separate requests. This ensures that chunk embeddings retain context from the entire document, improving semantic quality. If a document exceeds the model's context limits, or if the document contains non-text inputs (relevant when using the multimodal `embedding` task), late chunking is automatically disabled for that document only and standard chunking is used instead. If not specified, defaults to `false`."
    },
    "__schema31": {
      "description": "For a `rerank` task, the number of most relevant documents to return. It defaults to the number of the documents. If this inference endpoint is used in a `text_similarity_reranker` retriever query and `top_n` is set, it must be greater than or equal to `rank_window_size` in the query.",
      "$ref": "#/$defs/integer"
    },
    "InferenceJinaAITaskSettings": {
      "type": "object",
      "properties": {
        "return_documents": {
          "$ref": "#/$defs/__schema28"
        },
        "input_type": {
          "$ref": "#/$defs/__schema29"
        },
        "late_chunking": {
          "$ref": "#/$defs/__schema30"
        },
        "top_n": {
          "$ref": "#/$defs/__schema31"
        }
      },
      "additionalProperties": false,
      "id": "InferenceJinaAITaskSettings"
    }
  }
}
