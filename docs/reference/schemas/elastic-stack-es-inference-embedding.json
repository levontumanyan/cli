{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "inference_id": {
      "$ref": "#/$defs/__schema0"
    },
    "timeout": {
      "$ref": "#/$defs/__schema1"
    },
    "embedding": {
      "$ref": "#/$defs/__schema6"
    }
  },
  "required": [
    "inference_id"
  ],
  "additionalProperties": false,
  "id": "InferenceEmbeddingRequest",
  "$defs": {
    "__schema0": {
      "description": "The inference Id",
      "$ref": "#/$defs/Id"
    },
    "Id": {
      "type": "string",
      "id": "Id"
    },
    "__schema1": {
      "$ref": "#/$defs/__schema2"
    },
    "__schema2": {
      "description": "Specifies the amount of time to wait for the inference request to complete.",
      "$ref": "#/$defs/Duration"
    },
    "Duration": {
      "anyOf": [
        {
          "$ref": "#/$defs/__schema3"
        },
        {
          "$ref": "#/$defs/__schema4"
        },
        {
          "$ref": "#/$defs/__schema5"
        }
      ],
      "id": "Duration"
    },
    "__schema3": {
      "type": "string"
    },
    "__schema4": {
      "type": "number",
      "const": -1
    },
    "__schema5": {
      "type": "number",
      "const": 0
    },
    "__schema6": {
      "$ref": "#/$defs/InferenceRequestEmbedding"
    },
    "InferenceRequestEmbedding": {
      "type": "object",
      "properties": {
        "input": {
          "$ref": "#/$defs/__schema7"
        },
        "input_type": {
          "$ref": "#/$defs/__schema15"
        },
        "task_settings": {
          "$ref": "#/$defs/__schema16"
        }
      },
      "required": [
        "input"
      ],
      "additionalProperties": false,
      "id": "InferenceRequestEmbedding"
    },
    "__schema7": {
      "description": "Inference input. Either a string, an array of strings, a `content` object, or an array of `content` objects. string example: ``` \"input\": \"Some text\" ``` string array example: ``` \"input\": [\"Some text\", \"Some more text\"] ``` `content` object example: ``` \"input\": {     \"content\": {       \"type\": \"image\",       \"format\": \"base64\",       \"value\": \"data:image/jpeg;base64,...\"     }   } ``` `content` object array example: ``` \"input\": [   {     \"content\": {       \"type\": \"text\",       \"format\": \"text\",       \"value\": \"Some text to generate an embedding\"     }   },   {     \"content\": {       \"type\": \"image\",       \"format\": \"base64\",       \"value\": \"data:image/jpeg;base64,...\"     }   } ] ```",
      "$ref": "#/$defs/InferenceEmbeddingInput"
    },
    "InferenceEmbeddingStringInput": {
      "anyOf": [
        {
          "$ref": "#/$defs/__schema8"
        },
        {
          "$ref": "#/$defs/__schema9"
        }
      ],
      "id": "InferenceEmbeddingStringInput"
    },
    "__schema8": {
      "type": "string"
    },
    "__schema9": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "InferenceEmbeddingContentInput": {
      "anyOf": [
        {
          "$ref": "#/$defs/InferenceEmbeddingContentObject"
        },
        {
          "$ref": "#/$defs/__schema14"
        }
      ],
      "id": "InferenceEmbeddingContentInput"
    },
    "InferenceEmbeddingContentObject": {
      "type": "object",
      "properties": {
        "content": {
          "$ref": "#/$defs/__schema10"
        }
      },
      "required": [
        "content"
      ],
      "additionalProperties": false,
      "id": "InferenceEmbeddingContentObject"
    },
    "__schema10": {
      "description": "An object containing the input data for the model to embed",
      "$ref": "#/$defs/InferenceEmbeddingContentObjectContents"
    },
    "__schema11": {
      "description": "The type of input to embed.",
      "$ref": "#/$defs/InferenceEmbeddingContentType"
    },
    "InferenceEmbeddingContentType": {
      "type": "string",
      "enum": [
        "text",
        "image"
      ],
      "id": "InferenceEmbeddingContentType"
    },
    "__schema12": {
      "description": "The format of the input. For the `text` type this must be `text`. For the `image` type, this must be `base64`. If not specified, this will default to `text` for the `text` type and `base64` for the `image` type.",
      "$ref": "#/$defs/InferenceEmbeddingContentFormat"
    },
    "InferenceEmbeddingContentFormat": {
      "type": "string",
      "enum": [
        "text",
        "base64"
      ],
      "id": "InferenceEmbeddingContentFormat"
    },
    "__schema13": {
      "type": "string",
      "description": "The value of the input to embed. For images, this must be a base64-encoded data URI, i.e. \"data:content/type;base64,...\""
    },
    "InferenceEmbeddingContentObjectContents": {
      "type": "object",
      "properties": {
        "type": {
          "$ref": "#/$defs/__schema11"
        },
        "format": {
          "$ref": "#/$defs/__schema12"
        },
        "value": {
          "$ref": "#/$defs/__schema13"
        }
      },
      "required": [
        "type",
        "value"
      ],
      "additionalProperties": false,
      "id": "InferenceEmbeddingContentObjectContents"
    },
    "__schema14": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/InferenceEmbeddingContentObject"
      }
    },
    "InferenceEmbeddingInput": {
      "anyOf": [
        {
          "$ref": "#/$defs/InferenceEmbeddingStringInput"
        },
        {
          "$ref": "#/$defs/InferenceEmbeddingContentInput"
        }
      ],
      "id": "InferenceEmbeddingInput"
    },
    "__schema15": {
      "type": "string",
      "description": "The input data type for the embedding model. Possible values include: * `SEARCH` * `INGEST` * `CLASSIFICATION` * `CLUSTERING` Not all models support all values. Unsupported values will trigger a validation exception. Accepted values depend on the configured inference service, refer to the relevant service-specific documentation for more info. > info > The `input_type` parameter specified on the root level of the request body will take precedence over the `input_type` parameter specified in `task_settings`."
    },
    "__schema16": {
      "description": "Task settings for the individual inference request. These settings are specific to the <task_type> you specified and override the task settings specified when initializing the service.",
      "$ref": "#/$defs/InferenceTaskSettings"
    },
    "InferenceTaskSettings": {
      "id": "InferenceTaskSettings"
    }
  }
}
