{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "task_type": {
      "$ref": "#/$defs/__schema0"
    },
    "inference_id": {
      "$ref": "#/$defs/__schema2"
    },
    "timeout": {
      "$ref": "#/$defs/__schema3"
    },
    "query": {
      "$ref": "#/$defs/__schema8"
    },
    "input": {
      "$ref": "#/$defs/__schema10"
    },
    "input_type": {
      "$ref": "#/$defs/__schema13"
    },
    "task_settings": {
      "$ref": "#/$defs/__schema15"
    }
  },
  "required": [
    "inference_id",
    "input"
  ],
  "additionalProperties": false,
  "id": "InferenceInferenceRequest",
  "$defs": {
    "__schema0": {
      "$ref": "#/$defs/__schema1"
    },
    "__schema1": {
      "description": "The type of inference task that the model performs.",
      "$ref": "#/$defs/InferenceTaskType"
    },
    "InferenceTaskType": {
      "type": "string",
      "enum": [
        "sparse_embedding",
        "text_embedding",
        "rerank",
        "completion",
        "chat_completion",
        "embedding"
      ],
      "id": "InferenceTaskType"
    },
    "__schema2": {
      "description": "The unique identifier for the inference endpoint.",
      "$ref": "#/$defs/Id"
    },
    "Id": {
      "type": "string",
      "id": "Id"
    },
    "__schema3": {
      "$ref": "#/$defs/__schema4"
    },
    "__schema4": {
      "description": "The amount of time to wait for the inference request to complete.",
      "$ref": "#/$defs/Duration"
    },
    "Duration": {
      "anyOf": [
        {
          "$ref": "#/$defs/__schema5"
        },
        {
          "$ref": "#/$defs/__schema6"
        },
        {
          "$ref": "#/$defs/__schema7"
        }
      ],
      "id": "Duration"
    },
    "__schema5": {
      "type": "string"
    },
    "__schema6": {
      "type": "number",
      "const": -1
    },
    "__schema7": {
      "type": "number",
      "const": 0
    },
    "__schema8": {
      "$ref": "#/$defs/__schema9"
    },
    "__schema9": {
      "type": "string",
      "description": "The query input, which is required only for the `rerank` task. It is not required for other tasks."
    },
    "__schema10": {
      "anyOf": [
        {
          "$ref": "#/$defs/__schema11"
        },
        {
          "$ref": "#/$defs/__schema12"
        }
      ],
      "description": "The text on which you want to perform the inference task. It can be a single string or an array. > info > Inference endpoints for the `completion` task type currently only support a single string as input."
    },
    "__schema11": {
      "type": "string"
    },
    "__schema12": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "__schema13": {
      "$ref": "#/$defs/__schema14"
    },
    "__schema14": {
      "type": "string",
      "description": "Specifies the input data type for the embedding model. The `input_type` parameter only applies to Inference Endpoints with the `embedding` or `text_embedding` task type. Possible values include: * `SEARCH` * `INGEST` * `CLASSIFICATION` * `CLUSTERING` Not all services support all values. Unsupported values will trigger a validation exception. Accepted values depend on the configured inference service, refer to the relevant service-specific documentation for more info. > info > The `input_type` parameter specified on the root level of the request body will take precedence over the `input_type` parameter specified in `task_settings`."
    },
    "__schema15": {
      "$ref": "#/$defs/__schema16"
    },
    "__schema16": {
      "description": "Task settings for the individual inference request. These settings are specific to the task type you specified and override the task settings specified when initializing the service.",
      "$ref": "#/$defs/InferenceTaskSettings"
    },
    "InferenceTaskSettings": {
      "id": "InferenceTaskSettings"
    }
  }
}
