{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "id": {
      "$ref": "#/$defs/__schema0"
    },
    "index": {
      "$ref": "#/$defs/__schema1"
    },
    "preference": {
      "$ref": "#/$defs/__schema2"
    },
    "realtime": {
      "$ref": "#/$defs/__schema4"
    },
    "refresh": {
      "$ref": "#/$defs/__schema6"
    },
    "routing": {
      "$ref": "#/$defs/__schema8"
    },
    "_source": {
      "$ref": "#/$defs/__schema12"
    },
    "_source_excludes": {
      "$ref": "#/$defs/__schema18"
    },
    "_source_includes": {
      "$ref": "#/$defs/__schema20"
    },
    "stored_fields": {
      "$ref": "#/$defs/__schema22"
    },
    "version": {
      "$ref": "#/$defs/__schema24"
    },
    "version_type": {
      "$ref": "#/$defs/__schema26"
    }
  },
  "required": [
    "id",
    "index"
  ],
  "additionalProperties": false,
  "id": "ExistsRequest",
  "$defs": {
    "__schema0": {
      "description": "A unique document identifier.",
      "$ref": "#/$defs/Id"
    },
    "Id": {
      "type": "string",
      "id": "Id"
    },
    "__schema1": {
      "description": "A comma-separated list of data streams, indices, and aliases. It supports wildcards (`*`).",
      "$ref": "#/$defs/IndexName"
    },
    "IndexName": {
      "type": "string",
      "id": "IndexName"
    },
    "__schema2": {
      "$ref": "#/$defs/__schema3"
    },
    "__schema3": {
      "type": "string",
      "description": "The node or shard the operation should be performed on. By default, the operation is randomized between the shard replicas. If it is set to `_local`, the operation will prefer to be run on a local allocated shard when possible. If it is set to a custom value, the value is used to guarantee that the same shards will be used for the same custom value. This can help with \"jumping values\" when hitting different shards in different refresh states. A sample value can be something like the web session ID or the user name."
    },
    "__schema4": {
      "$ref": "#/$defs/__schema5"
    },
    "__schema5": {
      "type": "boolean",
      "description": "If `true`, the request is real-time as opposed to near-real-time."
    },
    "__schema6": {
      "$ref": "#/$defs/__schema7"
    },
    "__schema7": {
      "type": "boolean",
      "description": "If `true`, the request refreshes the relevant shards before retrieving the document. Setting it to `true` should be done after careful thought and verification that this does not cause a heavy load on the system (and slow down indexing)."
    },
    "__schema8": {
      "$ref": "#/$defs/__schema9"
    },
    "__schema9": {
      "description": "A custom value used to route operations to a specific shard.",
      "$ref": "#/$defs/Routing"
    },
    "Routing": {
      "anyOf": [
        {
          "$ref": "#/$defs/__schema10"
        },
        {
          "$ref": "#/$defs/__schema11"
        }
      ],
      "id": "Routing"
    },
    "__schema10": {
      "type": "string"
    },
    "__schema11": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "__schema12": {
      "$ref": "#/$defs/__schema13"
    },
    "__schema13": {
      "description": "Indicates whether to return the `_source` field (`true` or `false`) or lists the fields to return.",
      "$ref": "#/$defs/SearchSourceConfigParam"
    },
    "__schema14": {
      "type": "boolean"
    },
    "__schema15": {
      "$ref": "#/$defs/Fields"
    },
    "Fields": {
      "anyOf": [
        {
          "$ref": "#/$defs/__schema16"
        },
        {
          "$ref": "#/$defs/__schema17"
        }
      ],
      "id": "Fields"
    },
    "__schema16": {
      "$ref": "#/$defs/Field"
    },
    "Field": {
      "type": "string",
      "id": "Field"
    },
    "__schema17": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/Field"
      }
    },
    "SearchSourceConfigParam": {
      "anyOf": [
        {
          "$ref": "#/$defs/__schema14"
        },
        {
          "$ref": "#/$defs/__schema15"
        }
      ],
      "id": "SearchSourceConfigParam"
    },
    "__schema18": {
      "$ref": "#/$defs/__schema19"
    },
    "__schema19": {
      "description": "A comma-separated list of source fields to exclude from the response. You can also use this parameter to exclude fields from the subset specified in `_source_includes` query parameter. If the `_source` parameter is `false`, this parameter is ignored.",
      "$ref": "#/$defs/Fields"
    },
    "__schema20": {
      "$ref": "#/$defs/__schema21"
    },
    "__schema21": {
      "description": "A comma-separated list of source fields to include in the response. If this parameter is specified, only these source fields are returned. You can exclude fields from this subset using the `_source_excludes` query parameter. If the `_source` parameter is `false`, this parameter is ignored.",
      "$ref": "#/$defs/Fields"
    },
    "__schema22": {
      "$ref": "#/$defs/__schema23"
    },
    "__schema23": {
      "description": "A comma-separated list of stored fields to return as part of a hit. If no fields are specified, no stored fields are included in the response. If this field is specified, the `_source` parameter defaults to `false`.",
      "$ref": "#/$defs/Fields"
    },
    "__schema24": {
      "$ref": "#/$defs/__schema25"
    },
    "__schema25": {
      "description": "Explicit version number for concurrency control. The specified version must match the current version of the document for the request to succeed.",
      "$ref": "#/$defs/VersionNumber"
    },
    "VersionNumber": {
      "id": "VersionNumber",
      "$ref": "#/$defs/long"
    },
    "long": {
      "type": "number",
      "id": "long"
    },
    "__schema26": {
      "$ref": "#/$defs/__schema27"
    },
    "__schema27": {
      "description": "The version type.",
      "$ref": "#/$defs/VersionType"
    },
    "VersionType": {
      "type": "string",
      "enum": [
        "internal",
        "external",
        "external_gte"
      ],
      "id": "VersionType"
    }
  }
}
