{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "deployment_id": {
      "type": "string",
      "description": "Identifier for the Deployment"
    },
    "ref_id": {
      "type": "string",
      "description": "User-specified RefId for the Resource (or '_main' if there is only one)"
    },
    "validate_only": {
      "type": "boolean",
      "description": "When `true`, does nothing except return the entries' allowlist and reloadability statuses."
    }
  },
  "required": [
    "deployment_id",
    "ref_id"
  ],
  "additionalProperties": {}
}
