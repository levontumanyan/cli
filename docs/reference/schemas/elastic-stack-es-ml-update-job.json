{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "job_id": {
      "$ref": "#/$defs/__schema0"
    },
    "allow_lazy_open": {
      "$ref": "#/$defs/__schema1"
    },
    "analysis_limits": {
      "$ref": "#/$defs/__schema3"
    },
    "background_persist_interval": {
      "$ref": "#/$defs/__schema5"
    },
    "custom_settings": {
      "$ref": "#/$defs/__schema10"
    },
    "categorization_filters": {
      "$ref": "#/$defs/__schema14"
    },
    "description": {
      "$ref": "#/$defs/__schema16"
    },
    "model_plot_config": {
      "$ref": "#/$defs/__schema18"
    },
    "model_prune_window": {
      "$ref": "#/$defs/__schema22"
    },
    "daily_model_snapshot_retention_after_days": {
      "$ref": "#/$defs/__schema24"
    },
    "model_snapshot_retention_days": {
      "$ref": "#/$defs/__schema26"
    },
    "renormalization_window_days": {
      "$ref": "#/$defs/__schema28"
    },
    "results_retention_days": {
      "$ref": "#/$defs/__schema30"
    },
    "groups": {
      "$ref": "#/$defs/__schema32"
    },
    "detectors": {
      "$ref": "#/$defs/__schema35"
    },
    "per_partition_categorization": {
      "$ref": "#/$defs/__schema49"
    }
  },
  "required": [
    "job_id"
  ],
  "additionalProperties": false,
  "id": "MlUpdateJobRequest",
  "$defs": {
    "__schema0": {
      "description": "Identifier for the job.",
      "$ref": "#/$defs/Id"
    },
    "Id": {
      "type": "string",
      "id": "Id"
    },
    "__schema1": {
      "$ref": "#/$defs/__schema2"
    },
    "__schema2": {
      "type": "boolean",
      "description": "Advanced configuration option. Specifies whether this job can open when there is insufficient machine learning node capacity for it to be immediately assigned to a node. If `false` and a machine learning node with capacity to run the job cannot immediately be found, the open anomaly detection jobs API returns an error. However, this is also subject to the cluster-wide `xpack.ml.max_lazy_ml_nodes` setting. If this option is set to `true`, the open anomaly detection jobs API does not return an error and the job waits in the opening state until sufficient machine learning node capacity is available."
    },
    "__schema3": {
      "$ref": "#/$defs/MlAnalysisMemoryLimit"
    },
    "MlAnalysisMemoryLimit": {
      "type": "object",
      "properties": {
        "model_memory_limit": {
          "$ref": "#/$defs/__schema4"
        }
      },
      "required": [
        "model_memory_limit"
      ],
      "additionalProperties": false,
      "id": "MlAnalysisMemoryLimit"
    },
    "__schema4": {
      "type": "string",
      "description": "Limits can be applied for the resources required to hold the mathematical models in memory. These limits are approximate and can be set per job. They do not control the memory used by other processes, for example the Elasticsearch Java processes."
    },
    "__schema5": {
      "$ref": "#/$defs/__schema6"
    },
    "__schema6": {
      "description": "Advanced configuration option. The time between each periodic persistence of the model. The default value is a randomized value between 3 to 4 hours, which avoids all jobs persisting at exactly the same time. The smallest allowed value is 1 hour. For very large models (several GB), persistence could take 10-20 minutes, so do not set the value too low. If the job is open when you make the update, you must stop the datafeed, close the job, then reopen the job and restart the datafeed for the changes to take effect.",
      "$ref": "#/$defs/Duration"
    },
    "Duration": {
      "anyOf": [
        {
          "$ref": "#/$defs/__schema7"
        },
        {
          "$ref": "#/$defs/__schema8"
        },
        {
          "$ref": "#/$defs/__schema9"
        }
      ],
      "id": "Duration"
    },
    "__schema7": {
      "type": "string"
    },
    "__schema8": {
      "type": "number",
      "const": -1
    },
    "__schema9": {
      "type": "number",
      "const": 0
    },
    "__schema10": {
      "$ref": "#/$defs/__schema11"
    },
    "__schema11": {
      "type": "object",
      "propertyNames": {
        "$ref": "#/$defs/__schema12"
      },
      "additionalProperties": {
        "$ref": "#/$defs/__schema13"
      },
      "description": "Advanced configuration option. Contains custom meta data about the job. For example, it can contain custom URL information as shown in Adding custom URLs to machine learning results."
    },
    "__schema12": {
      "type": "string"
    },
    "__schema13": {},
    "__schema14": {
      "$ref": "#/$defs/__schema15"
    },
    "__schema15": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "__schema16": {
      "$ref": "#/$defs/__schema17"
    },
    "__schema17": {
      "type": "string",
      "description": "A description of the job."
    },
    "__schema18": {
      "$ref": "#/$defs/MlModelPlotConfig"
    },
    "MlModelPlotConfig": {
      "type": "object",
      "properties": {
        "annotations_enabled": {
          "$ref": "#/$defs/__schema19"
        },
        "enabled": {
          "$ref": "#/$defs/__schema20"
        },
        "terms": {
          "$ref": "#/$defs/__schema21"
        }
      },
      "additionalProperties": false,
      "id": "MlModelPlotConfig"
    },
    "__schema19": {
      "type": "boolean",
      "description": "If true, enables calculation and storage of the model change annotations for each entity that is being analyzed."
    },
    "__schema20": {
      "type": "boolean",
      "description": "If true, enables calculation and storage of the model bounds for each entity that is being analyzed."
    },
    "__schema21": {
      "description": "Limits data collection to this comma separated list of partition or by field values. If terms are not specified or it is an empty string, no filtering is applied. Wildcards are not supported. Only the specified terms can be viewed when using the Single Metric Viewer.",
      "$ref": "#/$defs/Field"
    },
    "Field": {
      "type": "string",
      "id": "Field"
    },
    "__schema22": {
      "$ref": "#/$defs/__schema23"
    },
    "__schema23": {
      "$ref": "#/$defs/Duration"
    },
    "__schema24": {
      "$ref": "#/$defs/__schema25"
    },
    "__schema25": {
      "description": "Advanced configuration option, which affects the automatic removal of old model snapshots for this job. It specifies a period of time (in days) after which only the first snapshot per day is retained. This period is relative to the timestamp of the most recent snapshot for this job. Valid values range from 0 to `model_snapshot_retention_days`. For jobs created before version 7.8.0, the default value matches `model_snapshot_retention_days`.",
      "$ref": "#/$defs/long"
    },
    "long": {
      "type": "number",
      "id": "long"
    },
    "__schema26": {
      "$ref": "#/$defs/__schema27"
    },
    "__schema27": {
      "description": "Advanced configuration option, which affects the automatic removal of old model snapshots for this job. It specifies the maximum period of time (in days) that snapshots are retained. This period is relative to the timestamp of the most recent snapshot for this job.",
      "$ref": "#/$defs/long"
    },
    "__schema28": {
      "$ref": "#/$defs/__schema29"
    },
    "__schema29": {
      "description": "Advanced configuration option. The period over which adjustments to the score are applied, as new data is seen.",
      "$ref": "#/$defs/long"
    },
    "__schema30": {
      "$ref": "#/$defs/__schema31"
    },
    "__schema31": {
      "description": "Advanced configuration option. The period of time (in days) that results are retained. Age is calculated relative to the timestamp of the latest bucket result. If this property has a non-null value, once per day at 00:30 (server time), results that are the specified number of days older than the latest bucket result are deleted from Elasticsearch. The default value is null, which means all results are retained.",
      "$ref": "#/$defs/long"
    },
    "__schema32": {
      "$ref": "#/$defs/__schema33"
    },
    "__schema33": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/__schema34"
      },
      "description": "A list of job groups. A job can belong to no groups or many."
    },
    "__schema34": {
      "type": "string"
    },
    "__schema35": {
      "$ref": "#/$defs/__schema36"
    },
    "__schema36": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/MlDetectorUpdate"
      },
      "description": "An array of detector update objects."
    },
    "MlDetectorUpdate": {
      "type": "object",
      "properties": {
        "detector_index": {
          "$ref": "#/$defs/__schema37"
        },
        "description": {
          "$ref": "#/$defs/__schema38"
        },
        "custom_rules": {
          "$ref": "#/$defs/__schema39"
        }
      },
      "required": [
        "detector_index"
      ],
      "additionalProperties": false,
      "id": "MlDetectorUpdate"
    },
    "__schema37": {
      "description": "A unique identifier for the detector. This identifier is based on the order of the detectors in the `analysis_config`, starting at zero.",
      "$ref": "#/$defs/integer"
    },
    "integer": {
      "type": "number",
      "id": "integer"
    },
    "__schema38": {
      "type": "string",
      "description": "A description of the detector."
    },
    "__schema39": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/MlDetectionRule"
      },
      "description": "An array of custom rule objects, which enable you to customize the way detectors operate. For example, a rule may dictate to the detector conditions under which results should be skipped. Kibana refers to custom rules as job rules."
    },
    "MlDetectionRule": {
      "type": "object",
      "properties": {
        "actions": {
          "$ref": "#/$defs/__schema40"
        },
        "conditions": {
          "$ref": "#/$defs/__schema41"
        },
        "scope": {
          "$ref": "#/$defs/__schema45"
        }
      },
      "additionalProperties": false,
      "id": "MlDetectionRule"
    },
    "__schema40": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/MlRuleAction"
      },
      "description": "The set of actions to be triggered when the rule applies. If more than one action is specified the effects of all actions are combined."
    },
    "MlRuleAction": {
      "type": "string",
      "enum": [
        "skip_result",
        "skip_model_update"
      ],
      "id": "MlRuleAction"
    },
    "__schema41": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/MlRuleCondition"
      },
      "description": "An array of numeric conditions when the rule applies. A rule must either have a non-empty scope or at least one condition. Multiple conditions are combined together with a logical AND."
    },
    "MlRuleCondition": {
      "type": "object",
      "properties": {
        "applies_to": {
          "$ref": "#/$defs/__schema42"
        },
        "operator": {
          "$ref": "#/$defs/__schema43"
        },
        "value": {
          "$ref": "#/$defs/__schema44"
        }
      },
      "required": [
        "applies_to",
        "operator",
        "value"
      ],
      "additionalProperties": false,
      "id": "MlRuleCondition"
    },
    "__schema42": {
      "description": "Specifies the result property to which the condition applies. If your detector uses `lat_long`, `metric`, `rare`, or `freq_rare` functions, you can only specify conditions that apply to time.",
      "$ref": "#/$defs/MlAppliesTo"
    },
    "MlAppliesTo": {
      "type": "string",
      "enum": [
        "actual",
        "typical",
        "diff_from_typical",
        "time"
      ],
      "id": "MlAppliesTo"
    },
    "__schema43": {
      "description": "Specifies the condition operator. The available options are greater than, greater than or equals, less than, and less than or equals.",
      "$ref": "#/$defs/MlConditionOperator"
    },
    "MlConditionOperator": {
      "type": "string",
      "enum": [
        "gt",
        "gte",
        "lt",
        "lte"
      ],
      "id": "MlConditionOperator"
    },
    "__schema44": {
      "description": "The value that is compared against the `applies_to` field using the operator.",
      "$ref": "#/$defs/double"
    },
    "double": {
      "type": "number",
      "id": "double"
    },
    "__schema45": {
      "type": "object",
      "propertyNames": {
        "$ref": "#/$defs/__schema46"
      },
      "additionalProperties": {
        "$ref": "#/$defs/MlFilterRef"
      },
      "description": "A scope of series where the rule applies. A rule must either have a non-empty scope or at least one condition. By default, the scope includes all series. Scoping is allowed for any of the fields that are also specified in `by_field_name`, `over_field_name`, or `partition_field_name`."
    },
    "__schema46": {
      "$ref": "#/$defs/Field"
    },
    "MlFilterRef": {
      "type": "object",
      "properties": {
        "filter_id": {
          "$ref": "#/$defs/__schema47"
        },
        "filter_type": {
          "$ref": "#/$defs/__schema48"
        }
      },
      "required": [
        "filter_id"
      ],
      "additionalProperties": false,
      "id": "MlFilterRef"
    },
    "__schema47": {
      "description": "The identifier for the filter.",
      "$ref": "#/$defs/Id"
    },
    "__schema48": {
      "description": "If set to `include`, the rule applies for values in the filter. If set to `exclude`, the rule applies for values not in the filter.",
      "$ref": "#/$defs/MlFilterType"
    },
    "MlFilterType": {
      "type": "string",
      "enum": [
        "include",
        "exclude"
      ],
      "id": "MlFilterType"
    },
    "__schema49": {
      "$ref": "#/$defs/__schema50"
    },
    "__schema50": {
      "description": "Settings related to how categorization interacts with partition fields.",
      "$ref": "#/$defs/MlPerPartitionCategorization"
    },
    "__schema51": {
      "type": "boolean",
      "description": "To enable this setting, you must also set the `partition_field_name` property to the same value in every detector that uses the keyword `mlcategory`. Otherwise, job creation fails."
    },
    "__schema52": {
      "type": "boolean",
      "description": "This setting can be set to true only if per-partition categorization is enabled. If true, both categorization and subsequent anomaly detection stops for partitions where the categorization status changes to warn. This setting makes it viable to have a job where it is expected that categorization works well for some partitions but not others; you do not pay the cost of bad categorization forever in the partitions where it works badly."
    },
    "MlPerPartitionCategorization": {
      "type": "object",
      "properties": {
        "enabled": {
          "$ref": "#/$defs/__schema51"
        },
        "stop_on_warn": {
          "$ref": "#/$defs/__schema52"
        }
      },
      "additionalProperties": false,
      "id": "MlPerPartitionCategorization"
    }
  }
}
