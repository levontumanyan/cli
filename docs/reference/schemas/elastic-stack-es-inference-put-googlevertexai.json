{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "task_type": {
      "$ref": "#/$defs/__schema0"
    },
    "googlevertexai_inference_id": {
      "$ref": "#/$defs/__schema1"
    },
    "timeout": {
      "$ref": "#/$defs/__schema2"
    },
    "chunking_settings": {
      "$ref": "#/$defs/__schema7"
    },
    "service": {
      "$ref": "#/$defs/__schema16"
    },
    "service_settings": {
      "$ref": "#/$defs/__schema17"
    },
    "task_settings": {
      "$ref": "#/$defs/__schema29"
    }
  },
  "required": [
    "task_type",
    "googlevertexai_inference_id",
    "service",
    "service_settings"
  ],
  "additionalProperties": false,
  "id": "InferencePutGooglevertexaiRequest",
  "$defs": {
    "__schema0": {
      "type": "string",
      "enum": [
        "rerank",
        "text_embedding",
        "completion",
        "chat_completion"
      ],
      "description": "The type of the inference task that the model will perform.",
      "$ref": "#/$defs/InferenceGoogleVertexAITaskType"
    },
    "InferenceGoogleVertexAITaskType": {
      "type": "string",
      "enum": [
        "rerank",
        "text_embedding",
        "completion",
        "chat_completion"
      ],
      "id": "InferenceGoogleVertexAITaskType"
    },
    "__schema1": {
      "description": "The unique identifier of the inference endpoint.",
      "$ref": "#/$defs/Id"
    },
    "Id": {
      "type": "string",
      "id": "Id"
    },
    "__schema2": {
      "$ref": "#/$defs/__schema3"
    },
    "__schema3": {
      "description": "Specifies the amount of time to wait for the inference endpoint to be created.",
      "$ref": "#/$defs/Duration"
    },
    "Duration": {
      "anyOf": [
        {
          "$ref": "#/$defs/__schema4"
        },
        {
          "$ref": "#/$defs/__schema5"
        },
        {
          "$ref": "#/$defs/__schema6"
        }
      ],
      "id": "Duration"
    },
    "__schema4": {
      "type": "string"
    },
    "__schema5": {
      "type": "number",
      "const": -1
    },
    "__schema6": {
      "type": "number",
      "const": 0
    },
    "__schema7": {
      "$ref": "#/$defs/__schema8"
    },
    "__schema8": {
      "description": "The chunking configuration object. Applies only to the `text_embedding` task type. Not applicable to the `rerank`, `completion`, or `chat_completion` task types.",
      "$ref": "#/$defs/InferenceInferenceChunkingSettings"
    },
    "__schema9": {
      "description": "The maximum size of a chunk in words. This value cannot be lower than `20` (for `sentence` strategy) or `10` (for `word` strategy). This value should not exceed the window size for the associated model.",
      "$ref": "#/$defs/integer"
    },
    "integer": {
      "type": "number",
      "id": "integer"
    },
    "__schema10": {
      "description": "The number of overlapping words for chunks. It is applicable only to a `word` chunking strategy. This value cannot be higher than half the `max_chunk_size` value.",
      "$ref": "#/$defs/integer"
    },
    "__schema11": {
      "description": "The number of overlapping sentences for chunks. It is applicable only for a `sentence` chunking strategy. It can be either `1` or `0`.",
      "$ref": "#/$defs/integer"
    },
    "__schema12": {
      "type": "string",
      "description": "Only applicable to the `recursive` strategy and required when using it. Sets a predefined list of separators in the saved chunking settings based on the selected text type. Values can be `markdown` or `plaintext`. Using this parameter is an alternative to manually specifying a custom `separators` list."
    },
    "__schema13": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/__schema14"
      },
      "description": "Only applicable to the `recursive` strategy and required when using it. A list of strings used as possible split points when chunking text. Each string can be a plain string or a regular expression (regex) pattern. The system tries each separator in order to split the text, starting from the first item in the list. After splitting, it attempts to recombine smaller pieces into larger chunks that stay within the `max_chunk_size` limit, to reduce the total number of chunks generated."
    },
    "__schema14": {
      "type": "string"
    },
    "__schema15": {
      "type": "string",
      "description": "The chunking strategy: `sentence`, `word`, `none` or `recursive`.  * If `strategy` is set to `recursive`, you must also specify: - `max_chunk_size` - either `separators` or`separator_group` Learn more about different chunking strategies in the linked documentation."
    },
    "InferenceInferenceChunkingSettings": {
      "type": "object",
      "properties": {
        "max_chunk_size": {
          "$ref": "#/$defs/__schema9"
        },
        "overlap": {
          "$ref": "#/$defs/__schema10"
        },
        "sentence_overlap": {
          "$ref": "#/$defs/__schema11"
        },
        "separator_group": {
          "$ref": "#/$defs/__schema12"
        },
        "separators": {
          "$ref": "#/$defs/__schema13"
        },
        "strategy": {
          "$ref": "#/$defs/__schema15"
        }
      },
      "additionalProperties": false,
      "id": "InferenceInferenceChunkingSettings"
    },
    "__schema16": {
      "type": "string",
      "enum": [
        "googlevertexai"
      ],
      "description": "The type of service supported for the specified task type. In this case, `googlevertexai`.",
      "$ref": "#/$defs/InferenceGoogleVertexAIServiceType"
    },
    "InferenceGoogleVertexAIServiceType": {
      "type": "string",
      "enum": [
        "googlevertexai"
      ],
      "id": "InferenceGoogleVertexAIServiceType"
    },
    "__schema17": {
      "type": "object",
      "properties": {
        "provider": {
          "$ref": "#/$defs/__schema18"
        },
        "url": {
          "$ref": "#/$defs/__schema19"
        },
        "streaming_url": {
          "$ref": "#/$defs/__schema20"
        },
        "location": {
          "$ref": "#/$defs/__schema21"
        },
        "model_id": {
          "$ref": "#/$defs/__schema22"
        },
        "project_id": {
          "$ref": "#/$defs/__schema23"
        },
        "rate_limit": {
          "$ref": "#/$defs/__schema24"
        },
        "service_account_json": {
          "$ref": "#/$defs/__schema26"
        },
        "dimensions": {
          "$ref": "#/$defs/__schema27"
        },
        "max_batch_size": {
          "$ref": "#/$defs/__schema28"
        }
      },
      "required": [
        "service_account_json"
      ],
      "additionalProperties": false,
      "description": "Settings used to install the inference model. These settings are specific to the `googlevertexai` service.",
      "$ref": "#/$defs/InferenceGoogleVertexAIServiceSettings"
    },
    "__schema18": {
      "description": "The name of the Google Model Garden Provider for `completion` and `chat_completion` tasks. In order for a Google Model Garden endpoint to be used `provider` must be defined and be other than `google`. Modes: - Google Model Garden (third-party models): set `provider` to a supported non-`google` value and provide `url` and/or `streaming_url`. - Google Vertex AI: omit `provider` or set it to `google`. In this mode, do not set `url` or `streaming_url` and Elastic will construct the endpoint url from `location`, `model_id`, and `project_id` parameters.",
      "$ref": "#/$defs/InferenceGoogleModelGardenProvider"
    },
    "InferenceGoogleModelGardenProvider": {
      "type": "string",
      "enum": [
        "google",
        "anthropic",
        "meta",
        "hugging_face",
        "mistral",
        "ai21"
      ],
      "id": "InferenceGoogleModelGardenProvider"
    },
    "__schema19": {
      "type": "string",
      "description": "The URL for non-streaming `completion` requests to a Google Model Garden provider endpoint. If both `url` and `streaming_url` are provided, each is used for its respective mode. If `streaming_url` is not provided, `url` is also used for streaming `completion` and `chat_completion`. If `provider` is not provided or set to `google` (Google Vertex AI), do not set `url` (or `streaming_url`). At least one of `url` or `streaming_url` must be provided for Google Model Garden endpoint usage. Certain providers require separate URLs for streaming and non-streaming operations (e.g., Anthropic, Mistral, AI21). Others support both operation types through a single URL (e.g., Meta, Hugging Face). Information on constructing the URL for various providers can be found in the Google Model Garden documentation for the model, or on the endpoint’s `Sample request` page. The request examples also illustrate the proper formatting for the `url`."
    },
    "__schema20": {
      "type": "string",
      "description": "The URL for streaming `completion` and `chat_completion` requests to a Google Model Garden provider endpoint. If both `streaming_url` and `url` are provided, each is used for its respective mode. If `url` is not provided, `streaming_url` is also used for non-streaming `completion` requests. If `provider` is not provided or set to `google` (Google Vertex AI), do not set `streaming_url` (or `url`). At least one of `streaming_url` or `url` must be provided for Google Model Garden endpoint usage. Certain providers require separate URLs for streaming and non-streaming operations (e.g., Anthropic, Mistral, AI21). Others support both operation types through a single URL (e.g., Meta, Hugging Face). Information on constructing the URL for various providers can be found in the Google Model Garden documentation for the model, or on the endpoint’s `Sample request` page. The request examples also illustrate the proper formatting for the `streaming_url`."
    },
    "__schema21": {
      "type": "string",
      "description": "The name of the location to use for the inference task for the Google Vertex AI inference task. For Google Vertex AI, when `provider` is omitted or `google` `location` is mandatory. For Google Model Garden's `completion` and `chat_completion` tasks, when `provider` is a supported non-`google` value - `location` is ignored. Refer to the Google documentation for the list of supported locations."
    },
    "__schema22": {
      "type": "string",
      "description": "The name of the model to use for the inference task. For Google Vertex AI `model_id` is mandatory. For Google Model Garden's `completion` and `chat_completion` tasks, when `provider` is a supported non-`google` value - `model_id` will be used for some providers that require it, otherwise - ignored. Refer to the Google documentation for the list of supported models for Google Vertex AI."
    },
    "__schema23": {
      "type": "string",
      "description": "The name of the project to use for the Google Vertex AI inference task. For Google Vertex AI `project_id` is mandatory. For Google Model Garden's `completion` and `chat_completion` tasks, when `provider` is a supported non-`google` value - `project_id` is ignored."
    },
    "__schema24": {
      "description": "This setting helps to minimize the number of rate limit errors returned from Google Vertex AI. By default, the `googlevertexai` service sets the number of requests allowed per minute to 30.000.",
      "$ref": "#/$defs/InferenceRateLimitSetting"
    },
    "__schema25": {
      "description": "The number of requests allowed per minute. By default, the number of requests allowed per minute is set by each service as follows: * `alibabacloud-ai-search` service: `1000` * `amazonbedrock` service: `240` * `anthropic` service: `50` * `azureaistudio` service: `240` * `azureopenai` service and task type `text_embedding`: `1440` * `azureopenai` service and task types `completion` or `chat_completion`: `120` * `cohere` service: `10000` * `contextualai` service: `1000` * `elastic` service and task type `chat_completion`: `240` * `fireworksai` service: `6000` * `googleaistudio` service: `360` * `googlevertexai` service: `30000` * `hugging_face` service: `3000` * `jinaai` service: `2000` * `llama` service: `3000` * `mistral` service: `240` * `openai` service and task type `text_embedding`: `3000` * `openai` service and task type `completion`: `500` * `openshift_ai` service: `3000` * `voyageai` service: `2000` * `watsonxai` service: `120`",
      "$ref": "#/$defs/integer"
    },
    "InferenceRateLimitSetting": {
      "type": "object",
      "properties": {
        "requests_per_minute": {
          "$ref": "#/$defs/__schema25"
        }
      },
      "additionalProperties": false,
      "id": "InferenceRateLimitSetting"
    },
    "__schema26": {
      "type": "string",
      "description": "A valid service account in JSON format for the Google Vertex AI API."
    },
    "__schema27": {
      "description": "For a `text_embedding` task, the number of dimensions the resulting output embeddings should have. By default, the model's standard output dimension is used. Refer to the Google documentation for more information.",
      "$ref": "#/$defs/integer"
    },
    "__schema28": {
      "description": "Only applicable for the `text_embedding` task type. Controls the batch size of chunked inference requests sent to Google Vertex AI. Setting this parameter lower reduces the risk of exceeding token limits but may result in more API calls. Setting it higher increases throughput but may risk hitting token limits. To estimate a safe `max_batch_size` value, you can use it together with the `max_chunk_size` parameter using the following formula: `max_batch_size ≈ max_chunk_size × 1.3 × 512 ÷ 20000` Where: - `1.3` is an approximate tokens-per-word ratio - `512` is the maximum number of chunks that can be generated per document - `20000` is the Google Vertex AI token limit per request This estimate assumes the worst-case scenario with a document generating the maximum 512 chunks.",
      "$ref": "#/$defs/integer"
    },
    "InferenceGoogleVertexAIServiceSettings": {
      "type": "object",
      "properties": {
        "provider": {
          "$ref": "#/$defs/__schema18"
        },
        "url": {
          "$ref": "#/$defs/__schema19"
        },
        "streaming_url": {
          "$ref": "#/$defs/__schema20"
        },
        "location": {
          "$ref": "#/$defs/__schema21"
        },
        "model_id": {
          "$ref": "#/$defs/__schema22"
        },
        "project_id": {
          "$ref": "#/$defs/__schema23"
        },
        "rate_limit": {
          "$ref": "#/$defs/__schema24"
        },
        "service_account_json": {
          "$ref": "#/$defs/__schema26"
        },
        "dimensions": {
          "$ref": "#/$defs/__schema27"
        },
        "max_batch_size": {
          "$ref": "#/$defs/__schema28"
        }
      },
      "required": [
        "service_account_json"
      ],
      "additionalProperties": false,
      "id": "InferenceGoogleVertexAIServiceSettings"
    },
    "__schema29": {
      "$ref": "#/$defs/__schema30"
    },
    "__schema30": {
      "description": "Settings to configure the inference task. These settings are specific to the task type you specified.",
      "$ref": "#/$defs/InferenceGoogleVertexAITaskSettings"
    },
    "__schema31": {
      "type": "boolean",
      "description": "For a `text_embedding` task, truncate inputs longer than the maximum token length automatically."
    },
    "__schema32": {
      "description": "For a `rerank` task, the number of the top N documents that should be returned.",
      "$ref": "#/$defs/integer"
    },
    "__schema33": {
      "description": "For a `completion` or `chat_completion` task, allows configuration of the thinking features for the model. Refer to the Google documentation for the allowable configurations for each model type.",
      "$ref": "#/$defs/InferenceThinkingConfig"
    },
    "__schema34": {
      "description": "Indicates the desired thinking budget in tokens.",
      "$ref": "#/$defs/integer"
    },
    "InferenceThinkingConfig": {
      "type": "object",
      "properties": {
        "thinking_budget": {
          "$ref": "#/$defs/__schema34"
        }
      },
      "additionalProperties": false,
      "id": "InferenceThinkingConfig"
    },
    "__schema35": {
      "description": "For `completion` and `chat_completion` tasks, specifies the `max_tokens` value for requests sent to the Google Model Garden `anthropic` provider. If `provider` is not set to `anthropic`, this field is ignored. If `max_tokens` is specified - it must be a positive integer. If not specified, the default value of 1024 is used. Anthropic models require `max_tokens` to be set for each request. Please refer to the Anthropic documentation for more information.",
      "$ref": "#/$defs/integer"
    },
    "InferenceGoogleVertexAITaskSettings": {
      "type": "object",
      "properties": {
        "auto_truncate": {
          "$ref": "#/$defs/__schema31"
        },
        "top_n": {
          "$ref": "#/$defs/__schema32"
        },
        "thinking_config": {
          "$ref": "#/$defs/__schema33"
        },
        "max_tokens": {
          "$ref": "#/$defs/__schema35"
        }
      },
      "additionalProperties": false,
      "id": "InferenceGoogleVertexAITaskSettings"
    }
  }
}
