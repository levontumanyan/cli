---
name: docs-preview-cleanup

on:
  pull_request_target:
    types:
      - closed

jobs:
  cleanup:
    uses: elastic/docs-actions/.github/workflows/docs-preview-cleanup.yml@b507b2c23531376aa8c6556bb3de0bdb25b1eb48 # v1
    permissions:
      contents: none
      id-token: write
      deployments: write
